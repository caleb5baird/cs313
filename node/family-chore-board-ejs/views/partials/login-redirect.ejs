<?php
//if they are not logged in forward them to the login page.
if (!isset($_SESSION['name']) || !isset($_SESSION['loggedIn'])) {
	$_SESSION['loggedIn'] = false;
	header("Location: login-page.php");
	exit();
}
?>
